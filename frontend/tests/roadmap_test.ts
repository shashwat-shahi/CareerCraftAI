// import React from 'react';
// import { mount } from 'enzyme';
// import SkillGapRoadmap from './SkillGapRoadmap';
 
// describe('SkillGapRoadmap', () => {
//   const userSkills = { current: ['HTML', 'CSS'], needed: ['JavaScript', 'React'] };
 
//   it('shows the current skills and skills to learn', () => {
//     const component = mount(<SkillGapRoadmap skills={userSkills.current} skillGaps={userSkills.needed} />);
//     expect(component.find('.current-skills-list').text()).toContain(userSkills.current.join(', '));
//     expect(component.find('.skills-to-learn-list').text()).toContain(userSkills.needed.join(', '));
//   });
// });
console.log("Hello World")